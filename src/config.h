#pragma once

// This setting is used to identify if PCIe ECAM is hardcoded to the q35 machine chipset ECAM

// #define HARDCODED_PCIE_ECAM

// This setting is used to identify if PMM logs every mark and allocation it does ( will hopefully be moved to bootargs in later versions! ) 

// #define PMM_VERBOSE

// This setting is used to identify if the kernel logs everything it does

#define KERNEL_VERBOSE

// This setting is used to identify if the OS is in developer mode

#define OS_DEV_MODE